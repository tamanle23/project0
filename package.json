{
  "name": "project0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "turbo": "^2.9.6",
    "prettier": "^3.8.1",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "esbuild": "0.18.20"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "0.18.20"
    }
  }
}
