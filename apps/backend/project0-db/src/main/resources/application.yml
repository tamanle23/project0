spring:
  main:
    web-application-type: none
  liquibase:
    enabled: true
    change-log: classpath:db/${schema}/changelog-master.xml
  datasource:
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

